<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.quangbui.gempicker</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/zsh</string>
        <string>-lc</string>
        <string>/Users/quangbui/Desktop/Gem Picker/scripts/run_daily.sh</string>
    </array>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>9</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <!-- Safe because run_daily.sh -> gempicker run is idempotent per day:
         if the Mac was asleep through 9am, logging in later still fires
         this job and it just runs that day's pick; if today already ran,
         it's a harmless no-op. This is what makes "never skip a day" work
         without needing a wake-scheduled cron. -->
    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/quangbui/Desktop/Gem Picker/data/logs/launchd.out.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/quangbui/Desktop/Gem Picker/data/logs/launchd.err.log</string>
</dict>
</plist>
